#include "renderer.h"

void renderPlayer(PositionComponent *positionComponent,
                  uint8_t playerEntityID) {
  float xPos = positionComponent->x[playerEntityID];
  float yPos = positionComponent->y[playerEntityID];
}
